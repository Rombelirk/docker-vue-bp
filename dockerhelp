docker container ls --> show all running containers

docker build -t "name" . ---> build image

docker run -it "tag" ---> run image as a container by tag

docker-compose up -d --no-deps --build ---> start compose with rebuilding containers